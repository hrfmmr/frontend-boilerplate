{
  "scripts": {
    "build": "yarn run build:js",
    "build:js": "webpack",
    "serve": "browser-sync start --server 'app' --port 4444 --files 'app'",
    "watch": "yarn run watch:js & yarn run serve",
    "watch:js": "yarn run build:js -- -w"
  },
  "devDependencies": {
    "babel-core": "^6.22.1",
    "babel-loader": "^6.2.10",
    "babel-plugin-transform-react-jsx": "^6.22.0",
    "browser-sync": "^2.18.7",
    "webpack": "^2.2.1"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}
